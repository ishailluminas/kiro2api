# ============================================================================
# kiro2api 环境配置文件示例
# ============================================================================
# 复制此文件为 .env 并根据需要修改配置

# ============================================================================
# Token 管理配置（必需）
# ============================================================================
#
# 支持两种配置方式：
#   1. 配置文件路径（推荐）- 指向 auth_config.json 文件
#   2. 直接配置 JSON 字符串
#
# 方式1：配置文件路径（推荐）
# 系统会自动检测是否为文件路径，优先从文件加载
KIRO_AUTH_TOKEN=auth_config.json

# 方式2：直接配置 JSON 字符串
# KIRO_AUTH_TOKEN='[{"auth":"Social","refreshToken":"your_refresh_token"}]'

# ============================================================================
# auth_config.json 文件格式示例
# ============================================================================
#
# 创建 auth_config.json 文件，内容如下：
#
# [
#   {
#     "auth": "Social",
#     "refreshToken": "aorAAAAAGj....."
#   },
#   {
#     "auth": "IdC",
#     "refreshToken": "aorAAAAAGj....",
#     "clientId": "uG-18bI....",
#     "clientSecret": "eyJraWQiOiJrZXktM....."
#   }
# ]
#
# 字段说明：
# - auth: 认证方式，"Social" 或 "IdC"
# - refreshToken: 刷新令牌（必需）
# - clientId: IdC 认证的客户端 ID（IdC 时必需）
# - clientSecret: IdC 认证的客户端密钥（IdC 时必需）
# - disabled: 是否禁用此配置（可选，默认 false）
#
# Token 获取方式：
# - Social tokens: ~/.aws/sso/cache/kiro-auth-token.json
# - IdC tokens: ~/.aws/sso/cache/ 目录下的相关 JSON 文件

# ============================================================================
# 基础服务配置
# ============================================================================

# 服务端口
PORT=8080

# API 认证密钥（客户端连接时使用）
KIRO_CLIENT_TOKEN=123456

# Gin 运行模式: debug, release, test
GIN_MODE=release

# ============================================================================
# Dashboard 控制台认证配置
# ============================================================================

# Dashboard 登录用户名
DASHBOARD_USERNAME=admin

# Dashboard 登录密码
DASHBOARD_PASSWORD=123456

# ============================================================================
# 日志配置
# ============================================================================

# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# 日志格式: text, json
LOG_FORMAT=json

# 日志文件路径（可选，不设置则只输出到控制台）
# LOG_FILE=/var/log/kiro2api.log

# ============================================================================
# 高级配置（可选）
# ============================================================================

# 工具描述的最大长度（字符数，默认: 10000）
# MAX_TOOL_DESCRIPTION_LENGTH=10000

# 自定义 Kiro 版本号（用于请求头伪装）
# KIRO_IDE_VERSION=0.8.0

# 自定义机器码（用于请求头伪装）
# KIRO_MACHINE_ID=your-custom-machine-id
